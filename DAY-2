====>    Array: 
•	Fixed-size data structure
•	Store multiple values of the same data type
•	Index starts with 0
Code:
	Int[] nums = [10,20,30];
	System.out.println(nums[1]); // Output: 20

====>    1-D Array:
•	Linear list of elements
Code: 
int[] num = new int[3];
arr[0] = 1;
arr[1]= 2;
arr[2] = 3;

for(int i=0; i<arr.length; i++)
{
		System.out.println(num[i]);
}

====>    2-D Array:
•	Represent rows and columns
Code:
	int[][] mat = {{1,2,3},{4,5,6}};
	System.out.println(matrix[1][2]); // Output: 6

====>    Method:
•	Blocks of code that perform a specific task
•	Improve reusability and modularity.

====>    Method Overloading:
•	Same name but different parameter.
•	Compile-time polymorphism.

====>    Method Overriding:
•	A subclass provides a specific implementation of a method that is already defined in the parent class.
•	Run-time polymorphism.

====>    String:
•	Immutable object-> once created, cannot be changed
•	Any modification creates a new object in the string constant pool or heap.

====>    StringBuilder:
•	Mutable and faster than a string.
•	Not thread-safe (not synchronised)

====>    StringBuffer:
•	Like StringBuilder but thread-safe
•	Slower than StringBuilder.
